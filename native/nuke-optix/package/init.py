"""Nuke startup hook for the native h_denoise_utils node package.

Both plugin DLLs and the isolated OIDN helper are discovered from this
``NUKE_PATH`` directory. No Python runtime dependency is required by either
node.
"""
